---
description: Review implemented changes for correctness, maintainability, and design alignment.
argument-hint: "Provide the change scope and design/plan references."
agent: sdp.reviewer
---

The `sdp.reviewer` agent will:
1. Review code for correctness, readability, and alignment with the approved design.
2. Check test quality and coverage.
3. Produce a report with categorized findings (Critical, Major, Minor).

A successful review is required before the security audit.
